{
  "name": "worms-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker + Durable Object backend for the worms web game",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.6.0",
    "vitest": "^1.6.1",
    "wrangler": "^3.90.0",
    "ws": "^8.20.0"
  }
}
